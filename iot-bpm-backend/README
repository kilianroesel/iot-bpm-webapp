docker run -d --restart always --name bpm_event_processing_db -p 5432:5432 -e POSTGRES_USER=admin -e POSTGRES_PASSWORD=secret -e POSTGRES_DB=bpm_event_processing -v postgres_data:/var/lib/postgresql/data/ postgres:alpine3.19